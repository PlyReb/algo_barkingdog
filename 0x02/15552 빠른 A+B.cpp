#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int T;
    cin >> T;

    int i = 0;
    int a, b;
    while (i <  T)
    {
        cin >> a >> b;
        cout << a + b << "\n";
        i++;
    }
}