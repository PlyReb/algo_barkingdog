#include <iostream>
#include <queue>

using namespace std;

string board[501][501];
int vis[501][501];

int n, m;
int dir_x[4] = { 1, 0, -1, 0 };
int dir_y[4] = { 0, 1, 0, -1 };

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);

	cin >> n >> m;

}