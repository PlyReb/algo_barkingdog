#include <iostream>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, m;
    int cnt = 0;
    int card[500005];
    

    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> card[i];
    }
}